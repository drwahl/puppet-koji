### THIS FILE IS MANAGED BY PUPPET
### <%= file.split(File::SEPARATOR)[5..-1].join(File::SEPARATOR) %>

# This is an ugly hack that I am not proud of, but it gets us off the ground. This
# should really be apart of the postgres module.

# TYPE  DATABASE    USER        CIDR-ADDRESS          METHOD
host    koji        koji    127.0.0.1/32      trust
host    koji        koji     ::1/128          trust
host    koji        koji    <%= @ipaddress %>/32    trust

# "local" is for Unix domain socket connections only
local   all         all                               trust
# IPv4 local connections:
host    all         all         127.0.0.1/32          ident
# IPv6 local connections:
host    all         all         ::1/128               ident

